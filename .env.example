API_PORT=3001
WEB_ORIGIN=http://localhost:5173
DATABASE_PATH=./data/guideanything.sqlite
UPLOAD_DIR=./data/uploads
JWT_SECRET=replace-with-at-least-32-characters-for-local-development
AGENT_RUNTIME_MODE=bridge
SANTEXWELL_VAULT_PATH=
AGENT_BRIDGE_URL=http://127.0.0.1:3010
# Local development only. Production must use a different secret with at least 32 characters.
AGENT_BRIDGE_TOKEN=guideanything-local-runtime-token-change-me
AGENT_MAX_CONCURRENCY=3
AGENT_ROUTER_TIMEOUT_MS=30000
AGENT_WORKER_TIMEOUT_MS=90000
AGENT_REDUCER_TIMEOUT_MS=90000
AGENT_RUN_TIMEOUT_MS=240000
# Semantic model roles are optional; the bridge resolves available models at runtime.
AGENT_MODEL_ROUTER=
AGENT_MODEL_DEEP_ROUTER=
AGENT_MODEL_FOCUSED_WORKER=
AGENT_MODEL_DEEP_WORKER=
AGENT_MODEL_REDUCER=
VITE_API_BASE=/api
